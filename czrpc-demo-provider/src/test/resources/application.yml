server:
  port: 8084
spring:
  application:
    name: czrpc-demo-test-provider
czrpc:
  # 应用级别的配置项
  app:
    applicationId: czrpc-demo-test
    namespace: public-test
    env: dev
  registryCenter:
    server: localhost:2181
    root: czrpcTest
logging:
  level:
    root: error
    com.cz: info